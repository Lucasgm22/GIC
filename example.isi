programa
    TEXTO a, f.
    REAL e.
	INTEIRO b,c,d.

	
	escreva("Hello World!").
	escreva("Digite o valor C").

	a := "Ola mundo aqui e a IsiLanguage".
	e := 3.14.
	b := 100 * ( 2 + 12 ) / 14.
	d := (b + 2) / 2.
	f := a.
	leia(c).
	escreva(a).
	escreva(f).
	escreva(b).
	escreva(e).
	escreva(d).
fimprog.