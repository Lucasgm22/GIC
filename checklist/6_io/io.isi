programa
    TEXTO a.
    INTEIRO b.
    REAL c.

    a := "TEXTO".
    b := 0.
    c := 0.1.

    escreva("ESCRITA").
    escreva(a).
    escreva(b).
    escreva(c).

    escreva("LEITURA").
    leia(a).
    leia(b).
    leia(c).

    escreva(a).
    escreva(b).
    escreva(c).

fimprog.