programa
    INTEIRO b, c.

    c := 0.
    b := 0.

    enquanto (b < 5) execute
        b := b + 1.
        c := 0.
        enquanto (c < 10) execute
            escreva(c).
            c := c + 2.
        fimenquanto.
        escreva(b).
    fimenquanto.
fimprog.